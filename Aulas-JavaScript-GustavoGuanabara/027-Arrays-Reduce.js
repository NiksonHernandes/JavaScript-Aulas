//REDUCE -> EXECUTA UMA FUNÇÃO REDUCER (FORNECIDA POR VOCÊ) PARA CADA ELEMENTO DO MEU ARRAY, RESULTANDO EM UM ÚNICO RETORNO;

//REDUZIR O MEU ARRAY A ALGO(UM OBJ, OUTRO ARRAY, O QUE EU QUISER )

const numeros = [1, 2, 3, 4, 5]

const soma = numeros.reduce()